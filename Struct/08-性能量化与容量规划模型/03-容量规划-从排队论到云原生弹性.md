# 容量规划：从排队论到云原生弹性

> **定位**：容量规划是从"猜需要多少机器"到"数学建模+自动化弹性"的转变。云原生时代，容量规划不再是静态的"买多少台服务器"，而是动态的"何时扩缩、扩缩多少"。
>
> **核心命题**：自动扩缩容（HPA/VPA）是排队论 Little定律 的实时实现——监控 L（当前负载），调整 c（服务台数量），维持 W（延迟）在目标范围内。

---

## 一、思维导图：容量规划方法论

```text
容量规划
│
├─【经典方法】
│   ├─ 基于峰值流量 × 冗余系数
│   ├─ 基于历史趋势线性外推
│   └─ 基于业务增长预测
│
├─【排队论方法】
│   ├─ M/M/c模型 → 确定最优服务台数c
│   ├─ Little定律 → L = λW 验证系统健康
│   └─ 目标利用率 ρ_target = λ/(cμ) < 0.7
│
├─【云原生弹性】
│   ├─ HPA（Horizontal Pod Autoscaler）
│   │   └─ 基于CPU/内存/自定义指标扩缩Pod数
│   ├─ VPA（Vertical Pod Autoscaler）
│   │   └─ 调整单个Pod的资源请求/限制
│   ├─ Cluster Autoscaler
│   │   └─ 调整节点数
│   └─ KEDA（基于事件驱动扩缩）
│
└─【关键指标】
    ├─ 饱和度（Saturation）
    ├─ 利用率（Utilization）
    ├─ 错误率（Errors）
    └─ 延迟（Latency）—— USE方法
```

---

## 二、排队论驱动的容量公式

```
目标：确定服务实例数 c，使得延迟满足SLA

已知：
  λ = 请求到达率（req/s）
  μ = 单个实例服务率（req/s）
  W_SLA = 目标平均延迟（秒）

M/M/c模型：
  利用率 ρ = λ / (cμ)

  平均等待时间（Erlang C公式）：
    Wq = P(wait > 0) × [1 / (cμ - λ)]

    其中 P(wait > 0) = ErlangC(c, λ/μ)

近似设计规则（ρ < 0.7时）：
  c ≈ ⌈λ/μ × safety_factor⌉

  safety_factor = 1 / ρ_target

  若 ρ_target = 0.7 → safety_factor ≈ 1.43

示例：
  λ = 1000 req/s, μ = 100 req/s per instance

  最小实例数 = 1000/100 = 10
  目标实例数（ρ=0.7）= 10 / 0.7 ≈ 15 instances
```

---

## 三、K8s HPA的数学原理

```
HPA目标：维持度量值 = target值

核心公式：
  desiredReplicas = ceil[currentReplicas × (currentMetricValue / targetMetricValue)]

示例（CPU-based）：
  currentReplicas = 10
  currentCPUUtilization = 80%
  targetCPUUtilization = 50%

  desiredReplicas = ceil[10 × (80/50)] = ceil[16] = 16

稳定性机制：
  - scaleUp stabilizationWindow: 防止抖动（默认0s）
  - scaleDown stabilizationWindow: 防止过早缩容（默认300s）
  - 最大扩缩比例限制

自定义指标（KEDA）：
  基于队列深度扩缩：
    desiredReplicas = ceil[queueLength / targetMessagesPerPod]

  示例：
    queueLength = 1000 messages
    targetMessagesPerPod = 100
    desiredReplicas = ceil[1000/100] = 10
```

---

## 四、容量规划检查清单

| 阶段 | **关键问题** | **工具/方法** |
|------|-----------|-------------|
| **需求分析** | 峰值QPS是多少？延迟SLA？ | 业务预估、历史数据分析 |
| **基线测试** | 单实例性能如何？ | 负载测试（k6, Locust, JMeter） |
| **模型计算** | 需要多少实例？ | M/M/c, Little定律 |
| **HPA配置** | 扩缩阈值？冷却时间？ | K8s HPA YAML, KEDA ScaledObject |
| **压力测试** | 2x/5x/10x峰值能否承受？ | 混沌工程、全链路压测 |
| **成本优化** | 资源利用率是否合理？ | Kubecost, AWS Cost Explorer |
| **持续监控** | 容量是否仍充足？ | Prometheus + Grafana dashboards |

---

## 五、概念定义与属性关系

| 概念 | 定义 | 属性 | 示例 | 反例 |
|------|------|------|------|------|
| **HPA** | K8s水平Pod自动扩缩 | 基于指标、反应式、秒级响应 | CPU>70%→扩容 | 手动扩缩（慢、易出错） |
| **VPA** | K8s垂直Pod自动扩缩 | 调整资源请求、需重启Pod | 内存不足→增加请求 | 固定资源分配 |
| **KEDA** | 基于事件驱动的K8s扩缩 | 支持多种事件源、云无关 | Kafka队列深度驱动扩缩 | 仅CPU/内存驱动 |
| **饱和点** | 系统性能开始非线性恶化的负载点 | 需实验确定、随代码变化 | 单实例>500rps时延迟飙升 | 线性扩展区域 |
| **USE方法** | 利用率、饱和度、错误率三指标 | 简洁、全面、可监控 | 资源监控Dashboard | 仅监控CPU |

---

## 六、交叉引用

- → [08-总览](./00-总览-从直觉到排队论.md)
- → [08/01-Little定律](01-排队论基础-Little定律与M_M_1队列.md)
- → [08/02-Amdahl-Gustafson](02-Amdahl-Gustafson-并行加速的理论边界.md)
- ↓ [12/02-容量规划场景](../../12-场景应用与决策框架/02-容量规划场景-从峰值到常态的弹性设计.md)

---

## 七、参考文献

| 作者 | 标题 | 出处 | 年份 |
|------|------|------|------|
| Brendan Gregg | *Systems Performance* (2nd ed.) | Addison-Wesley | 2020 |
| Neil Gunther | *Guerrilla Capacity Planning* | Springer | 2007 |
| K8s文档 | Horizontal Pod Autoscaler | kubernetes.io | 持续更新 |
| KEDA文档 | Kubernetes Event-driven Autoscaling | keda.sh | 持续更新 |

---

*文件创建日期：2026-04-23*
*状态：已完成*
