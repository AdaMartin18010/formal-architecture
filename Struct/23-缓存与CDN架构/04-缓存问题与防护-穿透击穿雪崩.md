# 缓存问题与防护：穿透、击穿、雪崩

> **来源映射**: View/00.md §2.1
> **国际权威参考**: "Bloom Filter Survey" (Tarkoma et al.), Facebook Memcache 论文, Netflix Hystrix 文档

---

## 一、知识体系思维导图

```text
缓存三大经典问题与防护体系
│
├─► 缓存穿透 (Cache Penetration)
│   ├─► 定义: 查询不存在的数据，请求直达 DB
│   ├─► 根源: 业务逻辑漏洞 / 恶意攻击
│   ├─► 防护
│   │   ├─► 布隆过滤器 (Bloom Filter): 概率性存在检测
│   │   ├─► 空值缓存 (Null Cache): 短暂缓存不存在的 key
│   │   └─► 输入校验与限流: 参数合法性检查
│   └─► 形式化: $\forall k \notin D, Query(k) \rightarrow DB$
│
├─► 缓存击穿 (Cache Breakdown)
│   ├─► 定义: 热点 key 过期瞬间，并发请求直达 DB
│   ├─► 根源: 高并发 + 热点 key TTL 到期
│   ├─► 防护
│   │   ├─► 互斥锁 (Mutex): 单线程回源
│   │   ├─► 逻辑过期 (Logical TTL): 永不过期 + 后台异步刷新
│   │   └─► 提前异步刷新: 在 TTL 到期前主动更新
│   └─► 形式化: $HotKey_{expire} \land Concurrent_{high} \rightarrow DB_{overload}$
│
└─► 缓存雪崩 (Cache Avalanche)
    ├─► 定义: 大量 key 同时过期，DB 压力骤增
    ├─► 根源: 批量导入相同 TTL / 缓存节点宕机
    ├─► 防护
    │   ├─► 随机化 TTL: base_ttl + random(0, delta)
    │   ├─► 多级缓存: L2 本地缓存兜底
    │   ├─► 熔断降级: Hystrix / Sentinel 快速失败
    │   └─► 热点预加载: 启动时预热 + 定期刷新
    └─► 形式化: $|Keys_{expire}(t)| \gg Threshold \rightarrow DB_{collapse}$
```

---

## 二、核心概念的形式化定义

**定义 1 (缓存穿透)**:
设键空间为 $K$，数据库中存在键集合为 $K_D$，缓存中存在键集合为 $K_C$:
$$Penetration(k) = k \notin K_D \land k \notin K_C \Rightarrow Query(k) \rightarrow DB$$
防护目标: $\forall k \notin K_D, P(Query(k) \rightarrow DB) < \epsilon$

**定义 2 (布隆过滤器)**:
布隆过滤器是概率型数据结构 $BF = \langle m, k, h \rangle$:

- $m$: 位数组长度
- $k$: 哈希函数个数
- $h = \{h_1, ..., h_k\}$: 哈希函数族

操作:

- $Add(x)$: 置位 $BF[h_i(x)] = 1, \forall i \in [1,k]$
- $Query(x)$: $\bigwedge_{i=1}^{k} BF[h_i(x)] = 1$

假阳性率:
$$P_{fp} \approx \left(1 - e^{-kn/m}\right)^k$$
最优 $k = \frac{m}{n} \ln 2$，此时 $P_{fp} \approx 0.6185^{m/n}$

**定义 3 (缓存击穿防护——互斥锁)**:
设热点键 $k^*$ 的过期时刻为 $t_e$，并发请求集合为 $R = \{r_1, ..., r_n\}$:
$$Mutex(R, k^*) = \exists! r_i \in R: r_i \text{ 获取锁并回源}, \forall r_j \neq r_i: r_j \text{ 阻塞或自旋}$$

**定义 4 (缓存雪崩)**:
$$Avalanche(t) = \left| \{ k \mid TTL(k) = t \} \right| > DB_{capacity}$$
防护: $\forall t, TTL(k_i) = t_0 + Uniform(0, \delta)$，使得键过期时间均匀分布

---

## 三、多维矩阵对比

| 问题 | 触发条件 | 影响范围 | 根因 | 核心防护手段 | 副作用 |
|------|---------|---------|------|-------------|--------|
| **穿透** | 查询不存在 key | 数据库查询压力 | 业务/攻击 | 布隆过滤器、空值缓存 | 布隆: 假阳性; 空值: 内存占用 |
| **击穿** | 热点 key 过期 | 单个 key 的 DB 压力 | 高并发+TTL | 互斥锁、逻辑过期 | 锁: 阻塞延迟; 逻辑过期: 数据滞后 |
| **雪崩** | 大量 key 同时过期 | 全量 DB 压力 | TTL 集中 | 随机 TTL、多级缓存、熔断 | 随机 TTL: 复杂度; 熔断: 功能降级 |

| 防护策略 | 实现复杂度 | 性能开销 | 防护效果 | 适用场景 |
|---------|-----------|---------|---------|---------|
| **布隆过滤器** | 中 | 极低(O(k)) | 高(可控假阳性) | 大量不存在的 key 查询 |
| **空值缓存(短TTL)** | 低 | 低 | 中 | 偶发的无效查询 |
| **互斥锁(分布式)** | 中 | 中(阻塞等待) | 高 | 确定性热点 key |
| **逻辑过期** | 中 | 低 | 高 | 极度热点且可容忍短暂旧值 |
| **随机 TTL** | 低 | 无 | 中 | 批量导入、定时任务 |
| **熔断降级** | 高 | 低 | 高 | 系统性风险兜底 |

---

## 四、权威引用

> **Burton H. Bloom** ("Space/Time Trade-offs in Hash Coding with Allowable Errors", CACM 1970):
> "The method uses hash coding to permit efficient storage and retrieval, with a small probability of false positives."

> **Facebook Engineering** ("Scaling Memcache at Facebook", NSDI 2013):
> "We use a lease mechanism to solve the thundering herd problem: the first client to miss gets a lease to refill the cache."

> **Netflix Tech Blog** ("Fault Tolerance in a High Volume Distributed System", 2012):
> "Hystrix wraps remote calls in circuit breakers, providing fallback behavior when services fail."

---

## 五、工程实践与代码示例

```python
import redis
import mmh3  # MurmurHash3
import bitarray
import threading
import time
import random

class BloomFilter:
    def __init__(self, size=2_000_000, hash_count=5):
        self.size = size
        self.hash_count = hash_count
        self.bit_array = bitarray.bitarray(size)
        self.bit_array.setall(0)

    def add(self, item):
        for i in range(self.hash_count):
            idx = mmh3.hash(item, i) % self.size
            self.bit_array[idx] = 1

    def query(self, item):
        return all(
            self.bit_array[mmh3.hash(item, i) % self.size]
            for i in range(self.hash_count)
        )

# 缓存击穿防护：互斥锁 + 逻辑过期
class HotKeyProtector:
    def __init__(self, redis_client):
        self.r = redis_client
        self.local_locks = {}

    def get_with_lock(self, key, loader, ttl=300):
        value = self.r.get(key)
        if value:
            return value

        # 获取分布式锁 (SET NX EX)
        lock_key = f"lock:{key}"
        lock_acquired = self.r.set(lock_key, "1", nx=True, ex=10)

        if lock_acquired:
            try:
                # 双重检查
                value = self.r.get(key)
                if not value:
                    value = loader(key)
                    # 随机化 TTL 防雪崩
                    jitter = random.randint(0, 60)
                    self.r.setex(key, ttl + jitter, value)
            finally:
                self.r.delete(lock_key)
            return value
        else:
            # 未获取锁，短暂自旋后重试
            time.sleep(0.1)
            return self.r.get(key) or loader(key)
```

---

## 六、批判性总结

缓存三大问题的本质，是**缓存作为中间层引入的时序不确定性**在不同场景下的具体表现。穿透是"无中生有"的攻击面，击穿是"新旧交替"的并发竞争，雪崩是"集体失效"的系统性风险。

布隆过滤器被广泛视为穿透问题的银弹，但一个常被忽略的细节是：**布隆过滤器不支持删除**。在数据动态变更的场景中，必须引入 Counting Bloom Filter 或 Cuckoo Filter 才能支持元素的删除操作，而这将使空间开销翻倍。此外，布隆过滤器的假阳性率与数据集规模正相关，在亿级键空间下维持 1% 以下的假阳性率需要约 10 倍于元素数量的位数组——这在内存敏感的场景中并非无代价。

互斥锁防护击穿的方案中，"谁持有锁回源"的设计暗含了一个**尾部延迟放大**问题：当回源操作本身超时（例如 DB 连接池耗尽），持有锁的线程长时间不释放锁，导致其他请求持续阻塞。Facebook 的 Memcache 论文提出的 **lease 机制**（租约令牌）是更优雅的方案——缓存 miss 时返回一个短期租约，允许持有租约的客户端回源，其他客户端要么等待要么使用过期数据，这种降级策略比纯阻塞更具备弹性。

雪崩防护中最被低估的是**熔断器**（Circuit Breaker）的作用。随机 TTL 和互斥锁解决的是"正常失效"，而熔断器解决的是"异常失效"——当 DB 响应时间 P99 超过阈值时，熔断器主动拒绝新的缓存 miss 请求，防止级联崩溃。这体现了分布式系统设计的核心原则：**快速失败优于缓慢拖垮**（fail-fast over fail-slow）。
