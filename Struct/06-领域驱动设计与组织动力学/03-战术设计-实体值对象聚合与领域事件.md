# 战术设计：实体、值对象、聚合与领域事件

> **定位**：DDD战术设计是将战略边界（Bounded Context）落地为可运行代码的模式语言。实体、值对象、聚合——这三个概念构成了领域模型的"语法"。
>
> **核心命题**：聚合是事务边界。违反聚合边界的事务设计，是分布式系统一致性灾难的根源。

---

## 一、思维导图：战术设计模式

```text
DDD战术设计
│
├─【领域对象类型】
│   ├─ Entity（实体）
│   │   └─ 有身份标识，随时间变化
│   ├─ Value Object（值对象）
│   │   └─ 无身份，不可变，由属性定义
│   └─ Domain Event（领域事件）
│       └─ 已发生的事实，不可变
│
├─【聚合（Aggregate）】
│   ├─ 根实体（Aggregate Root）
│   ├─ 内部对象（实体/值对象）
│   └─ 事务边界：一个事务只修改一个聚合
│
├─【仓储（Repository）】
│   └─ 聚合的持久化抽象
│
├─【服务（Domain Service）】
│   └─ 跨聚合的操作
│
└─【工厂（Factory）】
    └─ 复杂聚合的创建逻辑
```

---

## 二、核心概念的形式化定义

### 2.1 实体（Entity）

```
Entity E 定义为：
  E = (id, A, B)

  其中：
    id = 全局唯一标识符（不变）
    A = 属性集合（可变）
    B = 行为集合（操作A的方法）

  身份判等：E₁ = E₂ ⟺ id(E₁) = id(E₂)
  （两个属性完全相同的实体，若id不同，仍是不同实体）

  示例：
    Order (id=12345, status=CREATED, items=[...])
    Order (id=12346, status=CREATED, items=[...]) ← 不同实体
```

### 2.2 值对象（Value Object）

```
Value Object V 定义为：
  V = (A)  // 无id

  判等：V₁ = V₂ ⟺ ∀a ∈ A: a(V₁) = a(V₂)
  （完全由属性定义，相同属性即同一对象）

  约束：不可变（Immutable）
    - 创建后不可修改
    - 修改 = 创建新实例

  示例：
    Money(amount=100, currency=USD)
    Address(street="123 Main St", city="NYC", zip="10001")

    Money + 10 = new Money(110, USD)  // 非修改，而是新建
```

### 2.3 聚合（Aggregate）

```
Aggregate A 定义为：
  A = (Root, Entities, VOs, Invariants)

  其中：
    Root = 聚合根实体（外部引用的唯一入口）
    Entities = 内部实体集合
    VOs = 内部值对象集合
    Invariants = 不变量规则集合（必须始终为真）

  核心约束：
    1. 事务边界：一个事务只修改一个聚合
    2. 引用规则：外部只能引用聚合根
    3. 删除规则：删除聚合根 = 删除整个聚合

  不变量示例：
    Order聚合：
      - totalAmount = Σ(item.price × item.quantity)
      - items非空（已下单必须有商品）
      - status状态机转换合法
```

---

## 三、实体 vs 值对象决策树

```
是否需要身份标识？
  ├── 是 → Entity
  │       └── 生命周期中属性会变化？
  │             ├── 是 → 典型Entity（User, Order）
  │             └── 否 → 可能是Entity（如SSN不变但有身份）
  └── 否 → Value Object
          └── 属性组合是否可能重复？
                ├── 是 → 典型VO（Money, Address）
                └── 否 → 考虑是否为Entity

经验法则：
  - 优先使用Value Object（更简单、线程安全、易测试）
  - 仅在需要跟踪身份或生命周期时使用Entity
```

---

## 四、聚合设计规则

```
Rule 1: 聚合尽量小
  - 大聚合 = 大事务范围 = 并发冲突
  - 经验：一个聚合的加载应在毫秒级

Rule 2: 聚合间通过ID引用
  - Order聚合持有 CustomerId，而非Customer对象
  - 打破此规则 → 大聚合、跨聚合事务

Rule 3: 一个事务 = 一个聚合
  - 若需修改两个聚合 → 使用领域事件 + 最终一致
  - 例外：真正的业务不变量跨聚合时（极罕见）

Rule 4: 聚合根是唯一的入口
  - 外部代码不能直接修改聚合内部对象
  - 只能通过聚合根的方法

反模式示例：
  × Order.Items[0].Price = 100  // 直接修改内部对象
  ✓ Order.UpdateItemPrice(0, Money.Of(100, USD))  // 通过根方法
```

---

## 五、概念定义与属性关系

| 概念 | 定义 | 属性 | 示例 | 反例 |
|------|------|------|------|------|
| **Entity** | 有唯一身份标识的领域对象 | 可变、由id判等 | Order, Customer | Money（无id） |
| **Value Object** | 由属性定义的无身份对象 | 不可变、由属性判等 | Money, Address, DateRange | User（有id） |
| **Aggregate** | 以根实体为中心的对象集群 | 事务边界、一致性边界 | Order（含Items, ShippingAddress） | 跨聚合直接引用 |
| **Domain Event** | 领域中已发生的事实 | 不可变、时态性、可订阅 | OrderPlaced, PaymentReceived | 命令（意图，非事实） |
| **Repository** | 聚合的持久化集合抽象 | 领域层、不暴露存储细节 | OrderRepository | 直接使用ORM Session |

---

## 六、交叉引用

- → [06-总览](./00-总览-DDD作为架构原子.md)
- → [06/01-限界上下文](01-限界上下文-架构原子的形式化定义.md)
- → [06/02-战略设计](02-战略设计-问题空间与解空间分离.md)
- → [05/03-EDA](../05-架构模式与部署单元光谱/03-事件驱动架构-解耦与一致性的设计哲学.md)
- ↓ [04/04-EventSourcing](../04-数据一致性代数结构/03-EventSourcing-不可变数据代数与版本诅咒.md)

---

## 七、参考文献

| 作者 | 标题 | 出处 | 年份 |
|------|------|------|------|
| Eric Evans | *Domain-Driven Design* | Addison-Wesley | 2003 |
| Vaughn Vernon | *Implementing Domain-Driven Design* | Addison-Wesley | 2013 |
| Scott Millett, Nick Tune | *Patterns, Principles, and Practices of DDD* | Wrox | 2015 |

---

*文件创建日期：2026-04-23*
*状态：已完成*
