# 消息队列模型：点对点与发布订阅

> **来源映射**: View/01.md §3.1, Struct/21-消息队列与事件流架构/00-总览-消息队列的形式化谱系.md
> **国际权威参考**: JMS 2.0 Specification (Oracle, JSR 343), AMQP 1.0 (ISO/IEC 19464), "Enterprise Integration Patterns" (Hohpe & Woolf, 2003)

---

## 一、知识体系思维导图

```text
消息队列通信模型
│
├─► 点对点模型 (Point-to-Point / Queue)
│   ├─► 拓扑结构
│   │   ├─ 生产者 → 队列 → 消费者 (1:1 消费)
│   │   └─ 消息被消费后即从队列移除
│   ├─► 语义保证
│   │   ├─ 每条消息恰好被一个消费者处理
│   │   └─ 支持负载均衡 (Work Queue)
│   └─► 代表实现
│       ├─ JMS Queue (ActiveMQ, HornetQ)
│       ├─ RabbitMQ Classic Queue
│       └─ Amazon SQS Standard/ FIFO
│
└─► 发布-订阅模型 (Pub-Sub / Topic)
    ├─► 拓扑结构
    │   ├─ 生产者 → Topic → 多个独立订阅者
    │   └─ 消息持久化于 Topic，订阅者独立消费
    ├─► 语义变体
    │   ├─ 持久订阅 (Durable): 离线可恢复
    │   ├─ 非持久订阅 (Non-Durable): 仅在线接收
    │   └─ 共享订阅 (Shared): 多个消费者共享一个订阅
    └─► 代表实现
        ├─ JMS Topic (ActiveMQ Artemis)
        ├─ Kafka Topic (分布式日志)
        ├─ RabbitMQ Exchange + Queue Binding
        └─ Redis Pub/Sub (内存广播，无持久化)
```

---

## 二、核心概念的形式化定义

### 2.1 点对点队列的形式化

```text
定义 (点对点队列 Q):
  Q 是一个有穷或无穷的消息序列: Q = [m₁, m₂, ..., mₙ]

  生产者操作:
    enqueue(Q, m): Q' = Q ++ [m]  (追加到尾部)

  消费者操作 (原子性出队):
    dequeue(Q): 若 Q ≠ ∅, 返回 ⟨m₁, Q'⟩ 其中 Q' = [m₂, ..., mₙ]
                否则返回 ⊥

  关键约束 (恰好一次消费):
    ∀m ∈ Q, 被消费次数 count(m) ∈ {0, 1}
    成功消费后: count(m) = 1 ∧ m ∉ Q
```

### 2.2 发布-订阅的形式化

```text
定义 (发布-订阅系统 PS):
  PS = ⟨Topics, Subscribers, Subscriptions, Messages⟩

  订阅关系:
    Subscriptions ⊆ Subscribers × Topics
    ∀s ∈ Subscribers, topics(s) = {t | (s,t) ∈ Subscriptions}

  投递函数:
    deliver(m, t): ∀s ∈ Subscribers: t ∈ topics(s) → s.receive(m)

  消息持久化约束 (仅对持久订阅):
    durable: Subscribers_durable ⊆ Subscribers
    ∀s ∈ Subscribers_durable, ∀m published to t ∈ topics(s):
      if s.offline during publish: buffer(m, s) until s.online
```

### 2.3 JMS vs AMQP 的核心差异

```text
JMS (Java Message Service):
  - API 规范，非协议
  - 两种域: Point-to-Point (Queue) 和 Pub-Sub (Topic)
  - 消息模型: Message = {Header, Properties, Body}

AMQP (Advanced Message Queuing Protocol):
  - 二进制线级协议 (ISO/IEC 19464)
  - 核心抽象: Exchange → Binding → Queue
  - Exchange 类型: direct, fanout, topic, headers
  - 路由键匹配: binding_key 与 routing_key 的匹配函数
```

---

## 三、多维矩阵对比

| 维度 | 点对点 (P2P) | 发布-订阅 (Pub-Sub) | JMS API | AMQP Protocol |
|------|-------------|-------------------|---------|--------------|
| **消息消费** | 恰好一个消费者 | 所有订阅者 | 两者均支持 | 通过 Exchange 类型模拟 |
| **消息持久** | 持久到队列 | 持久到 Topic + 订阅 | 支持 | 支持 (Queue 持久化) |
| **负载均衡** | 天然支持 | 需共享订阅 | Queue 天然支持 | 多个 Consumer 绑定同一 Queue |
| **时间耦合** | 解耦 | 解耦更强 | 支持 | 支持 |
| **空间耦合** | 解耦 | 完全解耦 | 支持 | 支持 |
| **协议层级** | 语义模型 | 语义模型 | API 规范 | 线级协议 |
| **跨语言** | 依赖 Broker | 依赖 Broker | 仅限 JVM | 任何语言 |
| **路由灵活性** | 低 (FIFO) | 中 (Topic 匹配) | 中 | **高** (Exchange+Binding) |

---

## 四、权威引用

> **Gregor Hohpe & Bobby Woolf** ("Enterprise Integration Patterns", Addison-Wesley, 2003):
> "A Message Channel is a virtual pipe that connects a sender to a receiver. Point-to-Point ensures exactly one receiver; Publish-Subscribe allows multiple receivers."

> **JMS 2.0 Specification** (JSR 343, Oracle, 2013):
> "The point-to-point messaging domain uses queues; the publish/subscribe messaging domain uses topics. A JMS provider must support both domains."

> **AMQP 1.0 Specification** (ISO/IEC 19464, 2014):
> "AMQP is an open standard for passing business messages between applications or organizations. It connects systems, feeds business processes with the information they need, and reliably transmits onward the instructions that achieve their goals."

---

## 五、工程实践与代码示例

### RabbitMQ: Exchange + Queue Binding 实现 Pub-Sub

```python
import pika

connection = pika.BlockingConnection(pika.ConnectionParameters('localhost'))
channel = connection.channel()

# 声明 fanout Exchange (广播)
channel.exchange_declare(exchange='logs', exchange_type='fanout')

# 每个消费者创建独占队列，绑定到 Exchange
result = channel.queue_declare(queue='', exclusive=True)
queue_name = result.method.queue
channel.queue_bind(exchange='logs', queue=queue_name)

# 发布消息
channel.basic_publish(exchange='logs', routing_key='', body=b'Hello World')
```

---

## 六、批判性总结

点对点与发布-订阅并非互斥对立，而是**同一连续光谱**的两端。AMQP 的精妙之处在于用 Exchange + Queue 的组合统一了两种语义：fanout Exchange 实现 Pub-Sub，而多个 Consumer 竞争消费同一 Queue 实现 P2P 负载均衡。这种设计的代价是增加了概念复杂度——开发者必须理解 Exchange、Binding、Queue 的三层抽象，而 JMS 仅需要 Queue/Topic 两个概念。RabbitMQ 在灵活性上胜出，Kafka 在吞吐量和持久化上胜出，但两者都面临一个共同挑战：**消息顺序性保证与并行消费之间存在根本性张力**。在 P2P 中，顺序消费限制了吞吐量；在 Pub-Sub 中，分区策略可能破坏全局顺序。工程实践中，不存在"完美的消息模型"，只有对业务一致性需求的精确匹配。
