# 设计系统的版本控制与变更管理

> **来源映射**: View/05.md §6.3
> **国际权威参考**: SemVer 2.0.0 Spec, "Design System Versioning" (Nathan Curtis, EightShapes), "API Evolution" (Martin Fowler), "Structuring Design Systems" (Brad Frost, 2022), Chromatic Docs (Storybook), "DesignOps Handbook" (DesignBetter.Co)

---

## 一、知识体系思维导图

```text
设计系统的版本控制与变更管理
│
├─► SemVer (语义化版本控制)
│   ├─ MAJOR: Breaking Change (不兼容的 API 修改)
│   ├─ MINOR: 向后兼容的功能新增
│   ├─ PATCH: 向后兼容的问题修复
│   └─ 预发布: 1.0.0-alpha.1, 1.0.0-beta.2
│
├─► 设计系统的 Breaking Change 分类
│   ├─► 令牌层面
│   │   ├─ 令牌删除或重命名
│   │   ├─ 令牌值变更 (颜色、间距的语义漂移)
│   │   └─ 令牌类型变更 (color → gradient)
│   │
│   ├─► 组件层面
│   │   ├─ 组件 API 变更 (props 删除/重命名)
│   │   ├─ 组件行为变更 (交互逻辑改变)
│   │   ├─ 组件移除 (deprecated → removed)
│   │   └─ DOM 结构变更 (影响 CSS 选择器)
│   │
│   └─► 主题层面
│       ├─ 主题变量映射变更
│       └─ 默认主题切换 (light → dark)
│
├─► 迁移策略
│   ├─ 自动化迁移 (Codemod)
│   ├─ 废弃周期 (Deprecation Timeline)
│   ├─ 兼容性层 (Adapter / Bridge)
│   └─ 并行版本共存 (npm dist-tag)
│
├─► 变更沟通
│   ├─ CHANGELOG (Keep a Changelog 规范)
│   ├─ 迁移指南 (Migration Guide)
│   ├─ 设计令牌 diff 报告
│   └─ 视觉回归测试 (Chromatic / Applitools)
│
└─► 发布工作流
    ├─ 独立版本 (Independent): 每个包独立 SemVer
    ├─ 固定版本 (Fixed): 所有包共享版本号
    └─ 金丝雀发布 (Canary): 自动预发布通道
```

---

## 二、核心概念的形式化定义

### 2.1 SemVer 作为设计空间的分层

```text
定义 (语义化版本):
  版本 v = (major, minor, patch, pre, build)

  版本序: v₁ < v₂ ⟺ 字典序比较 (major, minor, patch)

  兼容性谓词:
    Compatible(v₁, v₂) ⟺
      v₁.major = v₂.major ∧ v₁.major > 0
      ∨ v₁.major = v₂.major = 0 ∧ v₁.minor = v₂.minor

    即: 同一 MAJOR 版本内向后兼容 (MAJOR > 0 时)
        0.y.z 的 MINOR 视为 MAJOR (开发阶段)

  设计系统的版本空间:
    每个设计系统 DS 的版本历史是一个全序集合:
      Versions(DS) = {v₁, v₂, ..., vₙ | v₁ < v₂ < ... < vₙ}

    变更类型函数:
      Δ: Versions(DS) × Versions(DS) → {MAJOR, MINOR, PATCH}
      Δ(vᵢ, vⱼ) = 从 vᵢ 到 vⱼ 的最大变更级别
```

### 2.2 Breaking Change 的形式化

```text
定义 (Breaking Change):
  设设计系统 DS 的公共接口为 API(DS) = Tokens ∪ Components ∪ Themes

  变更 δ: API(DS_v) → API(DS_v')

  δ 是 Breaking Change ⟺
    ∃consumer, consumer 在 v 下合法的使用方式在 v' 下非法

  具体判定:
    1. 令牌删除: t ∈ Tokens(v), t ∉ Tokens(v')  → MAJOR
    2. 令牌重命名: t ↦ t', 无 alias  → MAJOR
    3. 令牌值漂移:
       若值变更影响视觉/布局且消费者无法覆盖 → MAJOR
       若值变更为 bug fix (如对比度修正) → PATCH
    4. 组件 props 删除: prop ∈ Props(v), prop ∉ Props(v') → MAJOR
    5. 组件 DOM 变更:
       若变更影响消费者 CSS 选择器 → MAJOR
       若仅为内部实现优化 → PATCH
    6. 默认行为变更:
       若变更影响无障碍/键盘导航 → MAJOR
       若仅为性能优化 → MINOR/PATCH
```

### 2.3 迁移成本模型

```text
定义 (迁移成本):
  对于消费者项目 P，从版本 v 迁移到 v' 的成本:
    Cost(P, v → v') = α × |BreakingChanges|
                      + β × |FilesAffected|
                      + γ × |ManualReviewHours|

    α: 每处 breaking change 的修复成本系数
    β: 每受影响文件的调整成本系数
    γ: 人工审查的小时成本

  自动化迁移 (Codemod):
    若 ∃ codemod: Code(v) → Code(v')，则:
      Cost_auto(P, v → v') = Cost(P, v → v') × (1 - automation_rate)

    其中 automation_rate ∈ [0, 1] 为 Codemod 的覆盖率
```

---

## 三、多维矩阵对比

### 3.1 版本策略对比

| 策略 | 版本模式 | 优点 | 缺点 | 适用场景 |
|------|---------|------|------|---------|
| **独立版本** | 每个包独立 SemVer | 精准表达变更，消费者按需升级 | 版本矩阵复杂，协调困难 | 大型 Monorepo (Material-UI) |
| **固定版本** | 所有包共享版本号 | 简单一致，易于沟通 | 小变更导致全量版本 bump | 小型设计系统 (Bootstrap) |
| **范围版本** | ^x.y.z 自动升级 | 自动获取修复和功能 | 意外引入 breaking change | 内部项目 (可控环境) |
| **锁定版本** | 精确版本 | 确定性构建 | 错过安全修复 | 生产关键系统 |
| **金丝雀通道** | x.y.z-canary.n | 早期反馈，持续集成 | 不稳定，不适合生产 | 活跃开发阶段 |

### 3.2 变更管理工具对比

| 工具 | 变更检测 | 可视化 diff | 自动化发布 | 回归测试 | 生态集成 |
|------|---------|------------|-----------|---------|---------|
| **Changesets** | ✅ Git + 手动描述 | ⚠️ 文本 | ✅ 自动 bump + 发布 | ❌ | 优秀 (pnpm/yarn/npm) |
| **Lerna** | ✅ Conventional Commits | ❌ | ✅ 自动发布 | ❌ | 中等 |
| **Beachball** | ✅ 变更文件 | ❌ | ✅ 自动 bump | ❌ | Microsoft 生态 |
| **Chromatic** | ⚠️ 手动 | ✅ 像素级 diff | ❌ | ✅ 核心功能 | Storybook 深度集成 |
| **Applitools** | ⚠️ 手动 | ✅ AI 辅助 diff | ❌ | ✅ 核心功能 | 多框架支持 |

---

## 四、权威引用

> **Tom Preston-Werner** (SemVer 作者, 2013):
> "SemVer is a simple set of rules and requirements that dictate how version numbers are assigned and incremented. These rules are based on but not necessarily limited to pre-existing widespread common practices in use in both closed and open-source software."

> **Nathan Curtis** ("Versioning Design Systems", EightShapes, 2019):
> "Design system versioning is not just about code. A change to a color token is a breaking change if it causes visual regression across 50 products."

> **Brad Frost** ("Structuring Design Systems", 2022):
> "A design system is a product serving products. Like any product, it needs a roadmap, versioning strategy, and deprecation policy."

> **Martin Fowler** ("API Evolution", 2008):
> "The key to evolving APIs is to never break existing consumers. When you must introduce a breaking change, do it by adding the new behavior alongside the old, and deprecating the old over a long timeline."

---

## 五、工程实践与代码示例

### 5.1 Changesets 配置与使用

```bash
# 安装
npm install @changesets/cli

# 初始化
npx changeset init

# 添加变更记录 (交互式)
npx changeset
# ? Which packages would you like to include? ...
# ? Which packages should have a major bump? ...
# ? Which packages should have a minor bump? ...
# ? Please enter a summary for this change: ...
```

```markdown
<!-- .changeset/funny-dogs-wait.md -->
---
"@myds/core": major
"@myds/tokens": minor
---

Remove deprecated `size` prop from Button component.
Migrate to `scale` prop for consistent sizing across components.
```

### 5.2 Codemod 自动化迁移

```javascript
// codemods/button-size-to-scale.js
// jscodeshift transform
module.exports = function(fileInfo, api) {
  const j = api.jscodeshift;
  const root = j(fileInfo.source);

  // 查找 <Button size="..." /> 并替换为 scale
  root.find(j.JSXOpeningElement, { name: { name: 'Button' } })
    .find(j.JSXAttribute, { name: { name: 'size' } })
    .forEach(path => {
      path.value.name.name = 'scale';
    });

  return root.toSource();
};

// 执行
// npx jscodeshift -t codemods/button-size-to-scale.js src/
```

### 5.3 废弃周期策略

```typescript
// 阶段 1: 标记废弃 (MINOR 版本)
interface ButtonProps {
  /** @deprecated Use `scale` instead. Will be removed in v3.0.0 */
  size?: 'sm' | 'md' | 'lg';
  scale?: 'compact' | 'default' | ' spacious';
}

// 阶段 2: 运行时警告 (下一个 MINOR)
function Button(props: ButtonProps) {
  if (props.size) {
    console.warn(
      '[@myds/core] Button "size" prop is deprecated. Use "scale" instead.'
    );
    // 兼容映射
    const scaleMap = { sm: 'compact', md: 'default', lg: 'spacious' };
    props.scale = scaleMap[props.size];
  }
  // ...
}

// 阶段 3: 彻底移除 (MAJOR 版本)
// interface ButtonProps {
//   scale: 'compact' | 'default' | 'spacious';  // size 已移除
// }
```

---

## 六、批判性总结

设计系统的版本控制远比普通软件库复杂，因为其**"API" 同时存在于代码层、视觉层和交互层**。一个颜色令牌的值变更（如将错误红色从 #FF0000 调整为 #DC2626）在代码层面是完全向后兼容的（变量名未变、类型未变），但在视觉层面可能是 breaking change——如果下游产品的品牌规范要求使用特定的红色色值。这导致 SemVer 的机械规则在设计系统领域需要**视觉语义的人工判定**：变更的兼容性不仅取决于代码接口的稳定性，还取决于设计决策的稳定性。

更深层的问题是**版本传播的级联效应**。当一个基础设计令牌（如 --spacing-unit）发生 MAJOR 变更时，所有依赖该令牌的组件、主题、甚至消费者的自定义样式都会受到影响。在大型组织中，这种级联可能导致数十个产品团队同时进行迁移，产生巨大的协调成本。解决方案是**语义版本的多级分解**：基础令牌层、组件层、主题层各自独立版本化，通过依赖范围（如组件依赖令牌层的 ^2.x）控制变更的传播速度。这正是 Material-UI (MUI) 采用独立版本策略的代数动机。

视觉回归测试（Chromatic、Applitools）为设计系统的版本控制提供了**形式化的 diff 能力**——将"这个变更是否 breaking"从主观判断转化为像素级比较的客观度量。但视觉回归也有盲区：它只能检测渲染输出的变化，无法检测无障碍属性（ARIA 角色）或交互行为（键盘焦点）的变更。完整的设计系统版本控制需要**多模态回归测试**的矩阵：视觉 + 无障碍 + 交互 + 性能，而这在当前工具链中仍是碎片化的。未来的设计系统 CI/CD 可能集成**形式化设计规范验证器**，在构建时自动判定变更的 SemVer 级别，消除人工判定的主观性和不一致性。


---

## 七、概念属性关系网络

```text
设计系统版本控制与变更管理概念属性关系网络
│
├─► 核心实体层
│   ├─ [版本 v = (major, minor, patch, pre, build)]
│   │   ├─ major ──→ 属性: {Breaking Change计数, 架构迁移}
│   │   ├─ minor ──→ 属性: {向后兼容功能, 废弃标记}
│   │   ├─ patch ──→ 属性: {Bug修复, 性能优化, 安全补丁}
│   │   └─ pre ──→ 属性: {alpha, beta, rc, 金丝雀}
│   │
│   ├─ [公共接口 API(DS) = Tokens ∪ Components ∪ Themes]
│   │   ├─ Tokens ──→ 属性: {名称, 值, 类型, 别名}
│   │   ├─ Components ──→ 属性: {Props, DOM结构, 行为, 样式}
│   │   └─ Themes ──→ 属性: {变量映射, 默认值, 平台适配}
│   │
│   ├─ [变更 δ: API(v) → API(v')]
│   │   ├─ 令牌删除 ──→ 属性: {MAJOR, 消费者引用失效}
│   │   ├─ 令牌重命名 ──→ 属性: {MAJOR, 需alias兼容层}
│   │   ├─ 令牌值漂移 ──→ 属性: {条件MAJOR, 视觉回归风险}
│   │   ├─ Props删除 ──→ 属性: {MAJOR, 类型检查失效}
│   │   └─ DOM结构变更 ──→ 属性: {条件MAJOR, CSS选择器影响}
│   │
│   └─ [迁移成本 Cost(P, v→v')]
│       ├─ 自动修复 ──→ 属性: {Codemod覆盖率, jscodeshift规则}
│       ├─ 人工审查 ──→ 属性: {受影响文件数, 视觉回归范围}
│       └─ 废弃周期 ──→ 属性: {MINOR标记, 运行时警告, MAJOR移除}
│
├─► 关系映射层
│   ├─ δ → Δ: 变更类型决定 SemVer 级别
│   ├─ API(v) → Consumer: 公共接口映射到消费者项目
│   ├─ Cost → Codemod: 迁移成本驱动自动化工具
│   ├─ VisualReg → δ: 视觉回归检测判定变更兼容性
│   └─ DeprecationTimeline → v: 废弃周期映射到版本发布计划
│
├─► 属性约束层
│   ├─ 兼容性: Compatible(v₁,v₂) ⟺ v₁.major=v₂.major ∧ v₁.major>0
│   ├─ 全序性: Versions(DS) = {v₁,...,vₙ | v₁<v₂<...<vₙ}
│   ├─ 变更单调: Δ(vᵢ,vⱼ) ∈ {MAJOR, MINOR, PATCH} 且不可降级
│   ├─ 废弃承诺: @deprecated 标记后至少保留 1 个 MAJOR 周期
│   └─ 可重现构建: lockfile + 精确版本 ⟹ 确定性产物
│
└─► 课程对齐层
    ├─ Stanford CS 247: 设计迭代与版本管理
    └─ MIT 6.831: 设计评估与迭代改进
```

---

## 八、形式化推理链

**推理命题 P**: 设计系统的 SemVer 版本控制需要扩展为"代码-视觉-交互"三维兼容性模型，因为传统 SemVer 的机械规则无法捕获设计决策变更在视觉层和交互层的 breaking 效应。

**推理链**:

```text
前提 A1 (Tom Preston-Werner, 2013): "SemVer is a simple set of rules that dictate how
                                      version numbers are assigned and incremented.
                                      These rules are based on widespread common practices
                                      in both closed and open-source software."
前提 A2 (Nathan Curtis, 2019): "Design system versioning is not just about code. A change
                                 to a color token is a breaking change if it causes visual
                                 regression across 50 products."
前提 A3 (Martin Fowler, 2008): "The key to evolving APIs is to never break existing
                                 consumers. When you must introduce a breaking change,
                                 do it by adding the new behavior alongside the old,
                                 and deprecating the old over a long timeline."

步骤 1: 传统 SemVer 的形式化局限
       版本 v = (major, minor, patch)
       兼容性: Compatible(v₁,v₂) ⟺ v₁.major = v₂.major ∧ v₁.major > 0
       问题: 该谓词仅基于"代码接口"判定，忽略"视觉接口"和"交互接口"
       例: 颜色令牌 --error-color 从 #FF0000 改为 #DC2626
           代码层面: 名称未变，类型未变 ⟹ PATCH (机械规则)
           视觉层面: 品牌规范可能要求特定红色 ⟹ MAJOR (设计语义)
       ∴ 设计系统需要多维兼容性判定

步骤 2: 由 A2，设计系统变更的三维判定模型
       设变更 δ 影响三个维度:
         δ_code: 代码接口变更 (props, 类型, 导出)
         δ_visual: 视觉输出变更 (像素差异, 色彩, 布局)
         δ_interaction: 交互行为变更 (焦点, 键盘, ARIA)
       SemVer 级别:
         Δ(δ) = max(Δ_code(δ_code), Δ_visual(δ_visual), Δ_interaction(δ_interaction))
       其中:
         Δ_code 遵循传统 SemVer
         Δ_visual 由视觉回归测试判定 (Chromatic pixel diff)
         Δ_interaction 由无障碍测试和交互测试判定

步骤 3: 由 A3，渐进式迁移的形式化策略
       废弃周期: MINOR 标记 @deprecated → 运行时警告 → MAJOR 移除
       Codemod 自动化: ∃ codemod: Code(v) → Code(v')
       迁移成本: Cost(P,v→v') = α|Breaking| + β|Files| + γ|Hours|
       自动化降低: Cost_auto = Cost × (1 - automation_rate)
       最优策略: 在旧接口旁引入新接口，并行存在至少一个 MAJOR 周期

步骤 4: 版本传播级联效应的数学模型
       设设计系统 DS 被 n 个消费者项目 {P₁,...,Pₙ} 使用
       基础令牌层变更 → 组件层变更 → 主题层变更
       传播延迟: 各层独立版本化通过依赖范围控制 (^x.y.z)
       级联成本: TotalCost = Σᵢ Cost(Pᵢ, v→v')
       缓解策略: 语义版本的多级分解 + 范围版本控制传播速度

步骤 5: 视觉回归测试的形式化角色
       设渲染函数: Render(component) → Image
       视觉差异: Diff(Image_v, Image_v') = pixel-wise Δ
       判定阈值: 若 Δ > threshold ⟹ δ_visual = MAJOR
       该机制将"兼容性判定"从主观判断转化为像素比较的客观度量
       但盲区: 无法检测 ARIA 变更、焦点行为、动画时序

结论: 设计系统的版本控制是一个"多模态兼容性判定问题"。
       代码接口的兼容性可由静态分析判定，视觉兼容性需像素级回归测试，
       交互兼容性需无障碍和键盘测试。三者中的最高级别决定 SemVer 变更。
       未来的设计系统 CI/CD 应集成形式化规范验证器，在构建时自动判定
       变更级别，消除人工判定的主观性和不一致性。
```

---

## 九、推理判定树/决策树

```text
设计系统变更 SemVer 判定决策树
│
├─► Q1: 是否存在代码接口删除/重命名?
│   ├─ 是 ──→ MAJOR (传统 SemVer)
│   └─ 否 ──→ 继续 Q2
│
├─► Q2: 视觉输出是否变更?
│   ├─ 是 ──→ 继续 Q3
│   └─ 否 ──→ 继续 Q4
│
├─► Q3: 视觉变更是否可被消费者覆盖?
│   ├─ 是 (主题变量可自定义) ──→ MINOR 或 PATCH
│   └─ 否 (硬编码值变更) ──→ MAJOR (视觉回归风险)
│
├─► Q4: 交互行为是否变更?
│   ├─ 是 (键盘导航/焦点/ARIA) ──→ MAJOR (无障碍合规)
│   └─ 否 ──→ 继续 Q5
│
├─► Q5: 是否新增功能?
│   ├─ 是 ──→ MINOR (向后兼容)
│   └─ 否 ──→ PATCH (Bug修复)
│
└─► Q6: 是否标记废弃?
    ├─ 是 ──→ MINOR (@deprecated 声明，保持向后兼容)
    └─ 否 ──→ 当前判定完成

设计系统发布策略决策树
│
├─► Q1: 包数量与关系?
│   ├─ 单一包 ──→ 独立版本 (简单 SemVer)
│   └─ 多包 Monorepo ──→ 继续 Q2
│
├─► Q2: 版本耦合策略?
│   ├─ 独立版本 ──→ Changesets (各包独立 bump，精准表达变更)
│   ├─ 固定版本 ──→ Lerna fixed mode (所有包共享版本号)
│   └─ 混合 ──→ 核心包固定，工具包独立 (如 MUI)
│
├─► Q3: 预发布需求?
│   ├─ 是 ──→ 金丝雀通道 (canary) + dist-tag (next/beta)
│   └─ 否 ──→ 直接发布 latest
│
├─► Q4: 迁移工具?
│   ├─ 简单 Props 重命名 ──→ jscodeshift Codemod
│   ├─ 复杂逻辑变更 ──→ 运行时兼容层 + 废弃警告
│   └─ 架构重构 ──→ 迁移指南 + 升级工作坊
│
└─► Q5: 沟通渠道?
    ├─ CHANGELOG (Keep a Changelog 规范)
    ├─ 迁移指南 (Migration Guide)
    ├─ 设计令牌 diff 报告
    └─ 视觉回归测试报告 (Chromatic)
```

---

## 十、国际课程对齐标注

> **📚 Stanford CS 247 Human-Computer Interaction Design Studio**
>
> - **对齐章节**: §2.1 SemVer 作为设计空间的分层, §2.2 Breaking Change 的形式化, §六 批判性总结
> - **课程要点**: "Design iteration, rapid prototyping, and evaluation of design solutions.
>   Presenting design work and responding to feedback."
> - **深度映射**: CS247 的设计工作室强调"迭代"和"反馈"。本文件将设计迭代形式化为
>   版本空间 Versions(DS) = {v₁ < v₂ < ... < vₙ}，每次迭代对应版本的一次 bump。
>   课程中关于"设计决策文档化"的实践与本文件设计令牌 diff 报告和 CHANGELOG 的
>   工程需求一致——两者都追求将"为什么变更"和"变更影响"显性化。CS247 中关于
>   "用户测试"的方法论与本文件视觉回归测试形成设计验证的对照：前者验证用户
>   体验，后者验证视觉一致性。

> **📚 MIT 6.831 User Interface Design and Implementation**
>
> - **对齐章节**: §2.3 迁移成本模型, §5.1 Changesets 配置, §5.2 Codemod 自动化迁移
> - **课程要点**: "Iterative design process with graded milestones. Techniques for
>   evaluating and measuring interface usability."
> - **深度映射**: MIT 6.831 的课程项目采用迭代设计流程（GR1-GR6 里程碑），学生需要
>   在每次迭代中展示设计变更并解释理由。本文件将这一流程工程化为 Changesets 和
>   自动化迁移工具：Changesets 要求开发者在变更时写下"变更摘要"，对应 6.831 中
>   "设计说明"的写作训练；Codemod 的自动化迁移对应"从原型到实现"的迭代优化。
>   课程中关于"可用性度量"（heuristic evaluation, user testing）与本文件视觉回归
>   测试共同构成了"设计质量的多维验证"框架。
